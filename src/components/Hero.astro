---
---

<!-- Hero Section with enhanced SEO and accessibility -->
<main id="main-content" role="main">
    <section 
        id="hero" 
        class="relative min-h-screen bg-cover bg-center bg-no-repeat pt-16" 
        style="background-image: url('/hero-section.jpg');"
        aria-labelledby="hero-heading"
        itemscope 
        itemtype="https://schema.org/SoftwareApplication"
    >
        <!-- Enhanced dark overlay with gradient -->
        <div class="absolute inset-0 bg-gradient-to-b from-black/80 via-black/60 to-black/80" aria-hidden="true"></div>
        
        <!-- Hero content -->
        <div class="relative z-10 flex items-center justify-center min-h-screen text-center text-white px-4 sm:px-6 lg:px-8">
            <div class="w-full max-w-4xl mx-auto">
                <!-- Main headline with enhanced SEO structure -->
                <header>
                    <h1 
                        id="hero-heading"
                        class="hero-title text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black mb-4 sm:mb-6 md:mb-8 mt-4 sm:mt-8 md:mt-16 leading-tight tracking-tight text-glow"
                        itemprop="name"
                    >
                        <span class="block" aria-label="Run to the Rhythm">Run to the Rhythm.</span>
                        <span class="block" aria-label="Achieve your Pace">Achieve your Pace.</span>
                    </h1>
                </header>
                
                <!-- Subtitle with semantic markup -->
                <p 
                    class="text-base sm:text-lg md:text-xl lg:text-2xl mb-6 sm:mb-8 md:mb-12 text-gray-100 max-w-2xl mx-auto font-medium leading-relaxed px-2"
                    itemprop="description"
                >
                    Pacebeats transforms every run by syncing your steps with the perfect song.
                    Feel the music adapt to your stride, push past your limits, and turn every workout into a rhythm-powered journey.
                </p>
                
                <!-- Enhanced CTA Button with accessibility -->
                <div class="mb-8 sm:mb-12 md:mb-16 px-4 flex justify-center" role="region" aria-label="Call to action">
                    <button 
                        class="bg-blue-600 hover:bg-blue-700 text-white px-6 sm:px-10 py-3 sm:py-4 rounded-full font-bold text-base sm:text-lg transition-all duration-300 shadow-xl hover:shadow-2xl hover:scale-105 transform max-w-xs"
                        type="button"
                        aria-label="Get started with Pacebeats - Download the app"
                        onclick="scrollToSection('features')"
                    >
                        <span aria-hidden="true">🚀</span>
                        Let's Get Moving
                        <span class="sr-only">- Scroll to features section</span>
                    </button>
                </div>

                <!-- Phone mockup with comprehensive accessibility -->
                <figure class="relative flex justify-center px-4" role="img" aria-labelledby="app-preview-caption">
                    <img 
                        src="/hero-section-1.svg" 
                        alt="Pacebeats mobile app interface showing real-time pace tracking, music synchronization, and running analytics dashboard"
                        class="rounded-2xl shadow-2xl w-32 sm:w-40 md:w-48 lg:w-80 max-w-full h-auto transform hover:scale-105 transition-transform duration-500"
                        loading="eager"
                        decoding="async"
                        width="320"
                        height="640"
                        itemprop="screenshot"
                    />
                    <figcaption id="app-preview-caption" class="sr-only">
                        Preview of the Pacebeats mobile application interface displaying running metrics and music controls
                    </figcaption>
                    <!-- Subtle glow effect behind phone -->
                    <div class="absolute inset-0 bg-blue-600/20 blur-3xl rounded-full scale-150 -z-10" aria-hidden="true"></div>
                </figure>

                <!-- Hidden content for SEO and screen readers -->
                <div class="sr-only">
                    <h2>Key Features of Pacebeats Running App</h2>
                    <ul>
                        <li itemprop="featureList">Real-time pace tracking and analytics</li>
                        <li itemprop="featureList">Smart music synchronization with running cadence</li>
                        <li itemprop="featureList">Spotify integration for seamless music experience</li>
                        <li itemprop="featureList">Smartwatch companion app support</li>
                        <li itemprop="featureList">Community challenges and social features</li>
                        <li itemprop="featureList">Personalized training plans and goals</li>
                    </ul>
                    
                    <div itemprop="applicationCategory">Health & Fitness</div>
                    <div itemprop="operatingSystem">iOS, Android</div>
                    <meta itemprop="offers" content="Free with premium features available">
                    <meta itemprop="url" content="https://www.pacebeats.top">
                </div>
            </div>
        </div>
        
        <!-- Enhanced scroll indicator with accessibility -->
        <div 
            class="absolute bottom-4 sm:bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce"
            role="button"
            tabindex="0"
            aria-label="Scroll down to see more content"
            onclick="scrollToSection('features')"
            onkeydown="handleScrollIndicatorKeydown(event)"
        >
            <div class="w-6 h-10 border-2 border-white/30 rounded-full flex justify-center cursor-pointer">
                <div class="w-1 h-3 bg-white/60 rounded-full mt-2 animate-pulse"></div>
            </div>
            <span class="sr-only">Click or press Enter to scroll to the next section</span>
        </div>
    </section>
</main>

<!-- JSON-LD Structured Data for Enhanced SEO -->
<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": "Pacebeats",
    "alternateName": "Pacebeats Running App",
    "description": "Transform every run by syncing your steps with the perfect song. Pacebeats adapts music to your stride, helping you push past limits and turn workouts into rhythm-powered journeys.",
    "url": "https://www.pacebeats.top",
    "applicationCategory": "HealthApplication",
    "operatingSystem": ["iOS", "Android"],
    "keywords": ["running app", "fitness tracker", "music synchronization", "pace tracking", "running companion", "workout app", "spotify integration"],
    "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD",
        "availability": "https://schema.org/InStock"
    },
    "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": "4.8",
        "ratingCount": "150",
        "bestRating": "5",
        "worstRating": "1"
    },
    "author": {
        "@type": "Organization",
        "name": "Pacebeats Team",
        "url": "https://www.pacebeats.top",
        "logo": "https://www.pacebeats.top/pacebeats-logo.png"
    },
    "publisher": {
        "@type": "Organization",
        "name": "Pacebeats",
        "logo": {
            "@type": "ImageObject",
            "url": "https://www.pacebeats.top/pacebeats-logo.png"
        }
    },
    "featureList": [
        "Real-time pace tracking and analytics",
        "Smart music synchronization with running cadence", 
        "Spotify integration for seamless music experience",
        "Smartwatch companion app support",
        "Community challenges and social features",
        "Personalized training plans and goals",
        "GPS route tracking",
        "Performance insights and trends"
    ],
    "screenshot": [
        "https://www.pacebeats.top/hero-section-1.svg",
        "https://www.pacebeats.top/app-screenshot-2.jpg",
        "https://www.pacebeats.top/app-screenshot-3.jpg"
    ],
    "downloadUrl": "https://apps.apple.com/app/pacebeats",
    "installUrl": "https://play.google.com/store/apps/details?id=com.pacebeats",
    "softwareVersion": "2.1.0",
    "datePublished": "2024-01-15",
    "dateModified": "2025-01-15",
    "softwareRequirements": "iOS 14.0+ or Android 8.0+",
    "memoryRequirements": "100MB",
    "storageRequirements": "250MB"
}
</script>

<!-- Breadcrumb JSON-LD for Navigation -->
<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
        {
            "@type": "ListItem",
            "position": 1,
            "name": "Home",
            "item": "https://www.pacebeats.top/"
        },
        {
            "@type": "ListItem", 
            "position": 2,
            "name": "Features",
            "item": "https://www.pacebeats.top/#features"
        },
        {
            "@type": "ListItem",
            "position": 3, 
            "name": "How It Works",
            "item": "https://www.pacebeats.top/#how-it-works"
        }
    ]
}
</script>

<style>
    /* Athletic typography styling */
    .hero-title {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        font-weight: 900;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
        letter-spacing: -0.02em;
    }
    
    /* Text glowing effect */
    .text-glow {
        color: #ffffff;
        text-shadow: 
            0 0 10px rgba(59, 130, 246, 0.5),
            0 0 20px rgba(59, 130, 246, 0.3),
            0 0 40px rgba(59, 130, 246, 0.2),
            2px 2px 4px rgba(0, 0, 0, 0.8);
        animation: textGlow 3s ease-in-out infinite alternate;
    }
    
    @keyframes textGlow {
        from {
            text-shadow: 
                0 0 10px rgba(59, 130, 246, 0.5),
                0 0 20px rgba(59, 130, 246, 0.3),
                0 0 40px rgba(59, 130, 246, 0.2),
                2px 2px 4px rgba(0, 0, 0, 0.8);
        }
        to {
            text-shadow: 
                0 0 15px rgba(59, 130, 246, 0.8),
                0 0 30px rgba(59, 130, 246, 0.5),
                0 0 50px rgba(59, 130, 246, 0.3),
                2px 2px 4px rgba(0, 0, 0, 0.8);
        }
    }
    
    /* Responsive typography scaling */
    @media (max-width: 768px) {
        .hero-title {
            font-size: 3rem;
            line-height: 0.85;
        }
    }
    
    @media (max-width: 480px) {
        .hero-title {
            font-size: 2.5rem;
            line-height: 0.9;
        }
    }
    
    /* Additional responsive fixes for the hero section */
    #hero {
        max-width: 100vw;
        overflow-x: hidden;
    }
    
    /* Ensure proper spacing with fixed navbar */
    @media (min-width: 768px) {
        #hero {
            padding-top: 4rem; /* Account for fixed navbar */
        }
    }
    
    /* Mobile adjustments */
    @media (max-width: 767px) {
        #hero {
            padding-top: 4rem; /* Account for mobile navbar */
        }
        
        .hero-title {
            font-size: 2.25rem !important;
            line-height: 1.1;
            margin-bottom: 1rem !important; /* Reduced spacing */
        }
        
        .text-base {
            font-size: 0.95rem;
        }
    }
    
    /* Very small screens */
    @media (max-width: 480px) {
        .hero-title {
            font-size: 1.875rem !important;
            margin-bottom: 0.75rem !important; /* Further reduced spacing */
        }
        
        .text-base {
            font-size: 0.875rem;
            margin-bottom: 1.5rem !important; /* Reduced spacing */
        }
    }
    
    /* Landscape mobile fix */
    @media (max-height: 600px) and (orientation: landscape) {
        #hero {
            min-height: 100vh;
            padding-top: 3rem;
        }
        
        .hero-title {
            font-size: 1.75rem !important;
            margin-bottom: 1rem;
        }
        
        .mb-8, .mb-12 {
            margin-bottom: 1rem;
        }
        
        .mb-12.sm\:mb-16, .mb-16 {
            margin-bottom: 1.5rem;
        }
    }
    
    /* Enhanced hover effects */
    button {
        position: relative;
        overflow: hidden;
    }
    
    button::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transition: left 0.5s;
    }
    
    button:hover::before {
        left: 100%;
    }
    
    /* Smooth scrolling enhancement */
    html {
        scroll-behavior: smooth;
    }
    
    /* Loading animation for hero */
    .hero-title span {
        display: inline-block;
        animation: fadeInUp 1s ease-out forwards;
        opacity: 0;
        transform: translateY(30px);
    }
    
    .hero-title span:first-child {
        animation-delay: 0.2s;
    }
    
    .hero-title span:last-child {
        animation-delay: 0.4s;
    }
    
    @keyframes fadeInUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    /* Performance optimizations */
    .hero-title,
    button {
        will-change: transform;
    }
    
    /* Accessibility improvements */
    @media (prefers-reduced-motion: reduce) {
        .hero-title span {
            animation: none;
            opacity: 1;
            transform: none;
        }
        
        .text-glow {
            animation: none;
        }
        
        .animate-bounce,
        .animate-pulse {
            animation: none;
        }
        
        * {
            transition-duration: 0.01ms !important;
        }
    }
    
    /* High contrast mode support */
    @media (prefers-contrast: high) {
        .hero-title {
            text-shadow: 3px 3px 0px #000000;
        }
        
        .text-glow {
            text-shadow: 3px 3px 0px #000000;
            animation: none;
        }
        
        .text-gray-100 {
            color: #ffffff;
        }
    }
    
    /* Focus styles for accessibility */
    button:focus-visible {
        outline: 3px solid #ffffff;
        outline-offset: 2px;
    }
    
    /* Enhanced accessibility styles */
    .sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        white-space: nowrap;
        border: 0;
    }
    
    /* Focus indicators for keyboard navigation */
    button:focus-visible,
    [role="button"]:focus-visible {
        outline: 3px solid #ffffff;
        outline-offset: 2px;
        box-shadow: 0 0 0 6px rgba(59, 130, 246, 0.5);
    }
    
    /* Enhanced contrast for text readability */
    @media (prefers-contrast: high) {
        .hero-title {
            text-shadow: 3px 3px 0px #000000, -1px -1px 0px #000000;
            color: #ffffff;
        }
        
        .text-glow {
            text-shadow: 3px 3px 0px #000000, -1px -1px 0px #000000;
            animation: none;
            color: #ffffff;
        }
        
        .text-gray-100 {
            color: #ffffff;
            text-shadow: 1px 1px 0px #000000;
        }
        
        button {
            border: 2px solid #ffffff;
        }
    }
    
    /* Improved keyboard navigation styles */
    [role="button"]:hover {
        transform: scale(1.1);
        transition: transform 0.2s ease;
    }
    
    /* Loading states for better UX */
    .hero-content-loading {
        opacity: 0;
        transform: translateY(20px);
        transition: opacity 0.6s ease, transform 0.6s ease;
    }
    
    .hero-content-loaded {
        opacity: 1;
        transform: translateY(0);
    }
    
    /* Responsive image optimization */
    img {
        object-fit: contain;
        object-position: center;
    }
</style>

<script>
    // Enhanced accessibility and performance functions
    function scrollToSection(sectionId: string) {
        const element = document.getElementById(sectionId);
        if (element) {
            // Announce navigation to screen readers
            const announcement = document.createElement('div');
            announcement.setAttribute('aria-live', 'polite');
            announcement.setAttribute('aria-atomic', 'true');
            announcement.className = 'sr-only';
            announcement.textContent = `Navigating to ${sectionId} section`;
            document.body.appendChild(announcement);
            
            // Smooth scroll with focus management
            element.scrollIntoView({ 
                behavior: 'smooth', 
                block: 'start' 
            });
            
            // Set focus to the section for screen readers
            setTimeout(() => {
                element.setAttribute('tabindex', '-1');
                element.focus();
                document.body.removeChild(announcement);
            }, 1000);
            
            // Analytics tracking
            if ('gtag' in window) {
                (window as any).gtag('event', 'scroll_to_section', {
                    'event_category': 'engagement',
                    'event_label': sectionId,
                    'value': 1
                });
            }
        }
    }
    
    // Enhanced keyboard navigation for scroll indicator
    function handleScrollIndicatorKeydown(event: KeyboardEvent) {
        if (event.key === 'Enter' || event.key === ' ') {
            event.preventDefault();
            scrollToSection('features');
        }
    }
    
    // Enhanced performance with intersection observer
    if ('IntersectionObserver' in window) {
        const heroObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('hero-content-loaded');
                    entry.target.classList.remove('hero-content-loading');
                    
                    // Announce content load to screen readers
                    const announcement = document.createElement('div');
                    announcement.setAttribute('aria-live', 'polite');
                    announcement.className = 'sr-only';
                    announcement.textContent = 'Pacebeats homepage content has loaded';
                    document.body.appendChild(announcement);
                    
                    setTimeout(() => {
                        if (document.body.contains(announcement)) {
                            document.body.removeChild(announcement);
                        }
                    }, 2000);
                }
            });
        }, {
            threshold: 0.1,
            rootMargin: '50px'
        });
        
        const heroSection = document.querySelector('#hero');
        if (heroSection) {
            heroSection.classList.add('hero-content-loading');
            heroObserver.observe(heroSection);
        }
    }
    
    // Optimized parallax effect with reduced motion support
    let ticking = false;
    function updateParallax() {
        if (window.innerWidth > 768 && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            const scrolled = window.pageYOffset;
            const heroSection = document.querySelector('#hero') as HTMLElement;
            if (heroSection && scrolled < window.innerHeight) {
                heroSection.style.transform = `translateY(${scrolled * 0.3}px)`;
            }
        }
        ticking = false;
    }
    
    window.addEventListener('scroll', () => {
        if (!ticking) {
            requestAnimationFrame(updateParallax);
            ticking = true;
        }
    }, { passive: true });
    
    // Preload critical resources for performance
    if ('requestIdleCallback' in window) {
        requestIdleCallback(() => {
            // Preload hero image
            const heroImg = new Image();
            heroImg.src = '/hero-section-1.svg';
            
            // Preload next section images
            const preloadImages = ['/features-preview.jpg', '/app-interface.jpg'];
            preloadImages.forEach(src => {
                const img = new Image();
                img.src = src;
            });
        });
    }
    
    // Enhanced error handling and fallbacks
    window.addEventListener('error', (e) => {
        if (e.target instanceof HTMLImageElement) {
            // Fallback for failed image loads
            e.target.style.display = 'none';
            console.warn('Image failed to load:', e.target.src);
        }
    });
    
    // Expose functions globally for onclick handlers
    (window as any).scrollToSection = scrollToSection;
    (window as any).handleScrollIndicatorKeydown = handleScrollIndicatorKeydown;
</script>