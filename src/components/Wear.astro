<section 
    class="relative min-h-screen bg-cover bg-center bg-no-repeat" 
    style="background-image: url('/wear1.jpg');"
    id="wear-os"
    aria-labelledby="wear-heading"
    itemscope 
    itemtype="https://schema.org/SoftwareApplication"
>
    <!-- Dark overlay for better text readability -->
    <div class="absolute inset-0 bg-gradient-to-b from-black/70 via-black/50 to-black/80" aria-hidden="true"></div>
    
    <!-- Content -->
    <div class="relative z-10 flex flex-col items-center justify-center min-h-screen text-center text-white px-6">
        <!-- Heading with enhanced SEO structure -->
        <header class="mb-8 animate-fade-up">
            <div class="inline-block mb-4">
                <span class="text-blue-400 font-semibold text-sm uppercase tracking-wider">Coming Soon</span>
            </div>
            <h2 
                id="wear-heading" 
                class="text-5xl md:text-6xl lg:text-7xl font-black leading-tight font-inter mb-4"
                itemprop="name"
            >
                <span class="block bg-gradient-to-r from-white via-blue-200 to-cyan-200 bg-clip-text text-transparent">Pacebeats Companion</span>
                <span class="block bg-gradient-to-r from-cyan-200 via-blue-200 to-white bg-clip-text text-transparent">for Wear OS</span>
            </h2>
            <p class="sr-only">
                Discover the upcoming Pacebeats Wear OS companion app featuring real-time pace feedback, adaptive music control, and recovery insights directly on your smartwatch.
            </p>
        </header>
   
        <!-- Description text with semantic markup -->
        <div class="mb-8 max-w-2xl animate-fade-up" style="animation-delay: 0.2s;" role="region" aria-label="Wear OS features description">
            <p 
                class="text-gray-300 text-lg md:text-xl leading-relaxed mb-4"
                itemprop="description"
            >
                The future of running is on your wrist. Soon, Pacebeats will sync seamlessly with Wear OS smartwatches, giving you real-time pace feedback, adaptive music control, and recovery insights — without reaching for your phone.
            </p>
            <div class="inline-flex items-center gap-2 bg-blue-500/10 border border-blue-500/30 rounded-full px-4 py-2" role="status" aria-live="polite">
                <svg class="w-4 h-4 text-blue-400 animate-pulse" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
                </svg>
                <span class="text-blue-400 text-sm font-medium" aria-label="Development status">Wear OS integration coming soon</span>
            </div>
        </div>
        
        <!-- Action buttons with enhanced accessibility -->
        <div class="flex flex-wrap justify-center gap-4 animate-fade-up" style="animation-delay: 0.4s;" role="group" aria-label="Wear OS actions">
            <button 
                class="group relative bg-white/10 backdrop-blur-sm border-2 border-white/30 hover:border-white/50 text-white px-6 py-3 rounded-xl font-semibold hover:bg-white/20 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-white focus:ring-opacity-50 hover:scale-105"
                type="button"
                aria-label="Learn about companion features"
                onclick="showWearFeatures()"
            >
                <span class="relative z-10">Companion Features</span>
            </button>
            <button 
                class="relative bg-gradient-to-r from-blue-500 to-cyan-500 text-white px-6 py-3 rounded-xl font-semibold transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-opacity-50 opacity-90 cursor-not-allowed"
                type="button"
                disabled
                aria-label="Wear OS integration coming soon"
            >
                <span class="relative z-10 flex items-center gap-2">
                    <svg class="w-4 h-4 animate-pulse" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
                    </svg>
                    Coming Soon
                </span>
            </button>
            <button 
                class="group relative bg-white/10 backdrop-blur-sm border-2 border-white/30 hover:border-white/50 text-white px-6 py-3 rounded-xl font-semibold hover:bg-white/20 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-white focus:ring-opacity-50 hover:scale-105"
                type="button"
                aria-label="Learn more about Wear OS integration"
                onclick="scrollToFeatures()"
            >
                <span class="relative z-10">Learn More</span>
            </button>
        </div>

        <!-- Hidden content for SEO -->
        <div class="sr-only">
            <h3>Upcoming Wear OS Features</h3>
            <ul itemprop="featureList">
                <li>Real-time pace feedback on your wrist</li>
                <li>Adaptive music control without phone interaction</li>
                <li>Recovery insights and heart rate monitoring</li>
                <li>Seamless smartwatch synchronization</li>
                <li>Hands-free running experience</li>
                <li>Voice commands and notifications</li>
            </ul>
            
            <div itemprop="operatingSystem">Wear OS</div>
            <div itemprop="applicationSubCategory">Smartwatch Companion App</div>
            <div itemprop="releaseNotes">Wear OS integration coming soon</div>
        </div>
    </div>

    <!-- Floating feature indicators -->
    <div class="absolute bottom-12 left-12 hidden lg:block animate-float" style="animation-delay: 0.5s;">
        <div class="group relative bg-gradient-to-br from-blue-500/20 to-cyan-500/20 backdrop-blur-xl px-4 py-3 rounded-xl border-2 border-blue-500/30 hover:border-blue-500/50 transition-all duration-300 hover:scale-110">
            <div class="absolute inset-0 bg-blue-500/10 rounded-xl blur-lg animate-pulse-slow"></div>
            <span class="relative text-white text-sm font-semibold flex items-center gap-2">
                <svg class="w-4 h-4 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                </svg>
                Real-time Sync
            </span>
        </div>
    </div>
    
    <div class="absolute bottom-24 right-16 hidden lg:block animate-float" style="animation-delay: 1s;">
        <div class="group relative bg-gradient-to-br from-blue-500/20 to-cyan-500/20 backdrop-blur-xl px-4 py-3 rounded-xl border-2 border-cyan-500/30 hover:border-cyan-500/50 transition-all duration-300 hover:scale-110">
            <div class="absolute inset-0 bg-cyan-500/10 rounded-xl blur-lg animate-pulse-slow"></div>
            <span class="relative text-white text-sm font-semibold flex items-center gap-2">
                <svg class="w-4 h-4 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
                </svg>
                Heart Rate Monitor
            </span>
        </div>
    </div>
</section>

<!-- JSON-LD Structured Data for Wear OS Features -->
<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": "Pacebeats Wear OS Companion",
    "description": "Upcoming smartwatch companion app for Pacebeats featuring real-time pace feedback, adaptive music control, and recovery insights on Wear OS devices",
    "operatingSystem": "Wear OS",
    "applicationSubCategory": "Smartwatch Companion App",
    "featureList": [
        "Real-time pace feedback on your wrist",
        "Adaptive music control without phone interaction", 
        "Recovery insights and heart rate monitoring",
        "Seamless smartwatch synchronization",
        "Hands-free running experience",
        "Voice commands and notifications"
    ],
    "releaseNotes": "Wear OS integration coming soon",
    "isPartOf": {
        "@type": "SoftwareApplication",
        "name": "Pacebeats",
        "url": "https://www.pacebeats.top"
    },
    "audience": {
        "@type": "Audience", 
        "audienceType": "Smartwatch users and running enthusiasts"
    },
    "developmentStatus": "In Development"
}
</script>

<style>
/* Enhanced typography */
.font-inter {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    font-feature-settings: 'cv02', 'cv03', 'cv04', 'cv11';
    letter-spacing: -0.025em;
}

/* Enhanced heading styling */
h2.font-inter {
    text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.8);
    font-weight: 900;
}

/* Enhanced accessibility styles */
.sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
}

/* Fade up animation */
@keyframes fadeUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.animate-fade-up {
    animation: fadeUp 0.8s ease-out forwards;
    opacity: 0;
}

/* Floating animation */
@keyframes float {
    0%, 100% {
        transform: translateY(0px);
    }
    50% {
        transform: translateY(-10px);
    }
}

.animate-float {
    animation: float 3s ease-in-out infinite;
    opacity: 0;
    animation: fadeIn 0.6s ease-out forwards, float 3s ease-in-out 0.6s infinite;
}

/* Pulse slow animation */
@keyframes pulseSlow {
    0%, 100% {
        opacity: 0.5;
    }
    50% {
        opacity: 0.8;
    }
}

.animate-pulse-slow {
    animation: pulseSlow 3s ease-in-out infinite;
}

/* Floating indicators animation */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* High contrast mode support */
@media (prefers-contrast: high) {
    h2.font-inter {
        text-shadow: 3px 3px 0px #000000;
        color: #ffffff;
    }
    
    .text-gray-300,
    .text-gray-400 {
        color: #ffffff;
    }
    
    button {
        border: 2px solid #ffffff;
    }
    
    button:focus {
        outline: 3px solid #ffffff;
    }
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
    .animate-fade-up,
    .animate-float {
        animation: none;
        opacity: 1;
        transform: none;
    }
    
    .animate-pulse,
    .animate-pulse-slow {
        animation: none;
    }
    
    button:hover {
        transform: none;
    }
}

/* Enhanced focus styles */
button:focus-visible {
    outline: 3px solid rgba(255, 255, 255, 0.8);
    outline-offset: 2px;
}

/* Responsive enhancements */
@media (max-width: 768px) {
    h2.font-inter {
        font-size: 2.5rem;
        line-height: 1.1;
    }
    
    .flex-wrap {
        flex-direction: column;
        align-items: center;
    }
    
    button {
        width: 100%;
        max-width: 280px;
    }
}

@media (max-width: 640px) {
    h2.font-inter {
        font-size: 2rem;
    }
}
</style>

<script>
    // Enhanced functions for button interactions
    function showWearFeatures() {
        // Announce to screen readers
        const announcement = document.createElement('div');
        announcement.setAttribute('aria-live', 'assertive');
        announcement.className = 'sr-only';
        announcement.textContent = 'Companion features information: Real-time pace feedback, adaptive music control, and recovery insights will be available on Wear OS devices';
        document.body.appendChild(announcement);
        
        // Analytics tracking
        if ('gtag' in window) {
            (window as any).gtag('event', 'wear_features_clicked', {
                'event_category': 'engagement',
                'event_label': 'wear_os_features',
                'value': 1
            });
        }
        
        setTimeout(() => {
            if (document.body.contains(announcement)) {
                document.body.removeChild(announcement);
            }
        }, 3000);
    }
    
    function scrollToFeatures() {
        const featuresSection = document.getElementById('features');
        if (featuresSection) {
            featuresSection.scrollIntoView({ 
                behavior: 'smooth', 
                block: 'start' 
            });
            
            // Announce navigation
            const announcement = document.createElement('div');
            announcement.setAttribute('aria-live', 'polite');
            announcement.className = 'sr-only';
            announcement.textContent = 'Navigating to features section';
            document.body.appendChild(announcement);
            
            setTimeout(() => {
                if (document.body.contains(announcement)) {
                    document.body.removeChild(announcement);
                }
            }, 2000);
        }
        
        // Analytics tracking
        if ('gtag' in window) {
            (window as any).gtag('event', 'learn_more_clicked', {
                'event_category': 'navigation',
                'event_label': 'wear_os_learn_more',
                'value': 1
            });
        }
    }
    
    // Enhanced keyboard navigation
    document.addEventListener('keydown', (e: KeyboardEvent) => {
        if (e.target instanceof HTMLButtonElement && (e.key === 'Enter' || e.key === ' ')) {
            e.preventDefault();
            e.target.click();
        }
    });
    
    // Intersection observer for floating indicators
    if ('IntersectionObserver' in window) {
        const wearObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    // Announce section load to screen readers
                    const announcement = document.createElement('div');
                    announcement.setAttribute('aria-live', 'polite');
                    announcement.className = 'sr-only';
                    announcement.textContent = 'Pacebeats Wear OS section has loaded';
                    document.body.appendChild(announcement);
                    
                    setTimeout(() => {
                        if (document.body.contains(announcement)) {
                            document.body.removeChild(announcement);
                        }
                    }, 2000);
                }
            });
        }, {
            threshold: 0.3,
            rootMargin: '50px'
        });
        
        const wearSection = document.getElementById('wear-os');
        if (wearSection) {
            wearObserver.observe(wearSection);
        }
    }
    
    // Error handling for background image
    window.addEventListener('load', () => {
        const section = document.querySelector('section[style*="wear1.jpg"]') as HTMLElement;
        if (section) {
            const img = new Image();
            img.onload = () => {
                console.log('Wear OS background image loaded successfully');
            };
            img.onerror = () => {
                console.warn('Wear OS background image failed to load');
                section.style.backgroundColor = '#1f2937';
            };
            img.src = '/wear1.jpg';
        }
    });
    
    // Expose functions globally for onclick handlers
    (window as any).showWearFeatures = showWearFeatures;
    (window as any).scrollToFeatures = scrollToFeatures;
</script>
